# convert_docx_to_pdf.py uses LibreOffice (soffice), no Python deps required.
# Install: brew install --cask libreoffice
#
# add_logo_to_pdfs.py requires:
pymupdf>=1.24.0
pypdf>=4.0.0
#
# certificate_template.py (Python 3.6: use reportlab 3.x; reinstall if ImportError: NormalDate)
reportlab>=3.6.8,<4
Pillow>=8.4,<9  # Pillow 9+ drops Python 3.6
